import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  ingredientsContent: {
    flexWrap: "wrap",
    flexDirection: "row",
    gap: 12,
    paddingBottom: 20,
  },
})
